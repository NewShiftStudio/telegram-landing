#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo '🏗️👷 Форматирование, линтинг и проверка типов в проекте перед коммитом.'

npm run check-format ||
(
    echo '🤢🤮🤢🤮 Проверка преттиера не пройдена. 🤢🤮🤢🤮
            Запусти `npm run format`, добавь изменения и попробуй закоммитить снова.';
    false;
)

npm run check-lint ||
(
        echo '😤🏀👋😤 Проверка ESLint не пройдена. 😤🏀👋😤 
                Запусти `npm run lint`, добавь изменения и попробуй закоммитить снова.'
        false; 
)

npm run check-types ||
(
    echo '🤡😂❌🤡 Проверка типов не пройдена. 🤡😂❌🤡
            Запусти `npm run check-types`, исправь все проблемы и попробуй закоммитить заново.'
    false;
)

echo '✅✅✅✅ Ладно, ты победил... Просто закоммичу. ✅✅✅✅'
